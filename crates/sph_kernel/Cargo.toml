[package]
name = "sph_kernel"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "dev_soil_sph — Wendland smoothing kernels for SPH (W, grad W). Pure, substrate-free."

[dependencies]
